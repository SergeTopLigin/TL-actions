name: file_export

on:
  workflow_dispatch:
  
jobs:
  build: # job_id
    runs-on: ubuntu-latest
    
    steps:
      - name: python check preset
        run: python --version
      - uses: actions/checkout@v4  # загрузка содержимого github/ на вирт машину
     # - name: list current folder
     #   run: ls
     # - name: Run script
     #   run: python proba.py
     # - name: list current folder
     #   run: ls
     # - name: open file
     #   run: cat file.txt
