name: date

on:
  workflow_dispatch:
  
jobs:
  build: # job_id
    runs-on: ubuntu-latest
    
    steps:
      - run: date
      # - name: Put current date into a variable
      #   run: echo "date" >> $env:GITHUB_ENV
        # $NOW=& Get-Date -format yyyy-MM-dd
        # echo "NOW=$NOW" >> $env:GITHUB_ENV
      - name: Use variable
        run: echo "$NOW"
        env:
          NOW: Hello
